import BillingHistoryClient from './BillingHistoryClient';

export const metadata = {
  title: '결제 내역 — Show Me The Plan',
};

export default function BillingHistoryPage() {
  return <BillingHistoryClient />;
}
