import { ACCOUNT_SETTINGS_GUIDE } from '@/content/guide/account-settings';
import GuideTimeline from './GuideTimeline';

export default function AccountSettingsGuide() {
  return <GuideTimeline {...ACCOUNT_SETTINGS_GUIDE} />;
}
