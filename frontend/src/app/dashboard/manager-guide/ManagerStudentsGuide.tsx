import { MANAGER_STUDENTS_GUIDE } from '@/content/manager-guide/students';
import GuideTimeline from '@/app/dashboard/guide/GuideTimeline';

export default function ManagerStudentsGuide() {
  return <GuideTimeline {...MANAGER_STUDENTS_GUIDE} />;
}
