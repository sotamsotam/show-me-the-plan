# Local test: APP_DOMAIN=localhost
# Production: APP_DOMAIN=your-domain.example.com
{$APP_DOMAIN} {
	# Local upload provider: serve Strapi /uploads through the app domain.
	handle /uploads/* {
		reverse_proxy strapi:1337
	}

	handle {
		reverse_proxy frontend:3000
	}
}
