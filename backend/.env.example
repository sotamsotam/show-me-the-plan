HOST=0.0.0.0
PORT=1337

# Secrets
APP_KEYS="toBeModified1,toBeModified2"
API_TOKEN_SALT=tobemodified
ADMIN_JWT_SECRET=tobemodified
TRANSFER_TOKEN_SALT=tobemodified
JWT_SECRET=tobemodified

# Database (sqlite for local dev | postgres for Docker/production)
DATABASE_CLIENT=sqlite
DATABASE_HOST=127.0.0.1
DATABASE_PORT=5432
DATABASE_NAME=strapi
DATABASE_USERNAME=strapi
DATABASE_PASSWORD=tobemodified
DATABASE_SSL=false
DATABASE_FILENAME=.tmp/data.db

# Comma-separated browser origins (Docker: set in root .env)
CORS_ORIGIN=http://localhost:3000,http://localhost

# NEIS Open API (https://open.neis.go.kr)
NEIS_KEY=your-neis-api-key-here

# NEIS timetable in-memory cache
NEIS_CACHE_ENABLED=true
NEIS_CACHE_TTL_HOURS=12
NEIS_CACHE_MAX_ENTRIES=2000

# Email (Brevo SMTP) — password reset
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_SECURE=false
BREVO_SMTP_USER=your-id@smtp-brevo.com
BREVO_SMTP_KEY=xsmtpsib-your-smtp-key
EMAIL_DEFAULT_FROM=your-verified-sender@example.com
EMAIL_DEFAULT_NAME=Show Me The Plan
# Billing (shared with Next.js BFF via x-billing-internal-secret)
BILLING_INTERNAL_SECRET=change-me-billing-internal-secret
BILLING_ENCRYPTION_KEY=change-me-32-characters-min-secret!!

# Ops internal API (shared with Next.js BFF via x-ops-internal-secret)
OPS_INTERNAL_SECRET=change-me-ops-internal-secret

# Web Push (study-plan todo alarms)
# Generate: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key
VAPID_SUBJECT=mailto:support@example.com

# Upload storage — see root .env.example (UPLOAD_PROVIDER, UPLOAD_S3_*)
UPLOAD_PROVIDER=local
